	<div class="item-box small-1 columns <%=current_user.payments.active.first.purchases.where(book_id: item.id).exists? ? 'purchased': 'non-purchased'%>">
	<%= item.title %><br>
	<%= item.author %><br>
	<%= item.price %><br>
	<%= button_to "訂購",book_purchases_path, params: { :book_id=>item.id}, remote: true , class: 'button expand', form_class: 'purchase-form'%>
	<%= link_to "取消訂購", book_purchase_path(item.id), method: :delete, remote: true, class: 'button secondary expand cancel-button' %>
	<div class="alert purchase-status"></div>
	</div>